name: gatepass_lints
description: Custom lint rules for the GatePass+ Flutter app. Forbids hard-coded Color(0x…) outside lib/theme/.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.5

dependencies:
  analyzer: ^7.0.0
  analyzer_plugin: ^0.13.0
  custom_lint_builder: ^0.7.6
