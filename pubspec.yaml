name: divine_app
description: "Community living — maintenance, visitors, and everyday society tools"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.1.10+27

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Icons
  cupertino_icons: ^1.0.8
  
  # State Management
  flutter_riverpod: ^2.6.1
  
  # Networking
  dio: ^5.7.0
  pretty_dio_logger: ^1.4.0
  
  # Local Storage
  shared_preferences: ^2.3.4
  
  # Routing
  go_router: ^15.1.0
  
  # UI & Animations
  flutter_animate: ^4.5.0
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1
  flutter_svg: ^2.0.10+1
  
  # Date & Time
  intl: any
  
  # Utils
  logger: ^2.5.0
  uuid: ^4.5.1
  
  # Image Picker & Camera
  image_picker: ^1.1.2
  
  # Firebase (Push Notifications & Analytics)
  firebase_core: ^3.12.0
  firebase_messaging: ^15.2.1
  firebase_analytics: ^11.3.5
  firebase_crashlytics: ^4.3.2
  
  # Device Info
  device_info_plus: ^10.1.2
  
  # Permissions
  permission_handler: ^11.3.1
  
  # URL Launcher
  url_launcher: ^6.3.1

  # Biometric login (settings + login screen)
  local_auth: ^2.3.0
  flutter_secure_storage: ^9.2.4

  # In-app legal pages
  webview_flutter: ^4.10.0
  flutter_markdown: ^0.7.7
  
  # Pull to Refresh
  pull_to_refresh: ^2.0.0
  
  # Charts
  fl_chart: ^0.70.1
  
  # Share
  share_plus: ^10.1.2
  open_filex: ^4.5.0

  # QR Code
  qr_flutter: ^4.1.0
  google_fonts: ^8.1.0
  pdf: ^3.12.0
  path_provider: ^2.1.5
  mobile_scanner: ^7.2.0
  flutter_local_notifications: ^21.0.0
  connectivity_plus: ^6.1.4
  file_picker: ^8.1.7

  # Payment gateway
  razorpay_flutter: ^1.3.7

  # Web support (Blob/AnchorElement for PDF download on web)
  web: ^1.1.0

  # App version info (reads installed version from APK/IPA manifest)
  package_info_plus: ^8.0.0

  # Native Android in-app update (Play Store check via Play Core)
  in_app_update: ^4.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting
  flutter_lints: ^6.0.0
  custom_lint: ^0.7.6
  gatepass_lints:
    path: packages/gatepass_lints

  # Code generation & testing
  build_runner: ^2.4.15
  mocktail: ^1.0.4

# path_provider_foundation 2.6.x uses package:objective_c (FFI). On some iOS 26.x
# simulators that fails to load (DOBJC_initializeApi / dlopen). 2.5.1 is the
# last classic plugin implementation without objective_c — safe for simulator + device.
dependency_overrides:
  path_provider_foundation: 2.5.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/legal/
    - assets/branding/
    - assets/splash/
    - assets/fonts/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
